<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.santimattius.template">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidPerformance">

        <!-- Note, that Activities are part of :benchmarkable module -->

        <!-- enable profiling for benchmarks -->
        <profileable
            android:shell="true"
            tools:targetApi="q" />
    </application>


</manifest>